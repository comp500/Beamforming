# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 1.0.1+1.19
maven_group = link.infra
archives_base_name = beamforming

# Dependencies are managed at gradle/libs.versions.toml
